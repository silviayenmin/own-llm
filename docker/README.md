# Docker Directory

This directory hosts Dockerfiles and composition setups to run our training and API service inside containerized environments.

## Files

- `Dockerfile`: Defines building a container layer with CUDA/CPU support, dependencies, and code configuration.
- `docker-compose.yml`: (Optional) Orchestrates running the FastAPI server and TensorBoard container together.
