# Experiments Directory

This folder holds experimental artifacts, including:
- Checkpoints (`.pt` or `.pth` model state dicts)
- TensorBoard run logs (written to `runs/`)
- Training session summaries

Note: Raw model checkpoints and logs are git-ignored, but the directory is tracked.
