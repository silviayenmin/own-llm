# Inference Directory

This directory implements generation and sampling strategies for output text.

## Modules

- `generate.py`: Main generation loop that processes prompts and produces predicted tokens.
- `sampling.py`: Implements sampling strategies like Greedy, Top-K, Top-P (nucleus), and temperature scaling.
