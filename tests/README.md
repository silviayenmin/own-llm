# Tests Directory

This directory contains unit tests for our transformer layers, tokenizer, model initialization, configuration loader, and training datasets.

## Framework

Tests are implemented using Pytest. Each custom layer should be verified for mathematical correctness (e.g., shapes, backprop, outputs).
