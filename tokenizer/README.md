# Tokenizer Directory

This directory contains BPE (Byte-Pair Encoding) or character-level tokenization code.

## Modules

- `train_tokenizer.py`: Script to train a custom tokenizer vocabulary on a text dataset.
- `tokenizer.py`: Tokenizer class that encodes raw strings to token IDs and decodes token IDs back to strings.
