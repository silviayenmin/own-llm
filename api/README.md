# API Directory

This directory wraps the trained Mini GPT model in a FastAPI web server to serve real-time predictions.

## Modules

- `main.py`: FastAPI server setup with endpoints for text generation (`/generate`), health checking (`/health`), and loading configs.
